### TASK DECOMPOSITION

I need to identify the specific files that require modification or creation for this greedy chain color assignment task. Based on the architecture:

1. **Core Implementation Files** (BabyShark-specific):
   - `Services/chrisCrossAppleSause.cs` - Worker initialization logic
   - `Managers/BabySharkMiningManager.cs` - Mining orchestration and greedy ordering
   - `Setup/InitialMapData.cs` - Map data generation with mineral ordering

2. **Supporting Files**:
   - `MicroTasks/` directory (contains mining tasks)
   - `Services/JitPrepositionService.cs` - Worker positioning logic
   - Any DTO files that define `OrderedMainMinerals` structure

3. **Files to Ignore**:
   - All Sharky framework files unless critically broken
   - Base StarCraft II API files
   - Standard library dependencies

