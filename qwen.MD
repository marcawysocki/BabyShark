# Qwen File-Mapping Directive

## Objective
Analyze the BabyShark codebase structure. Identify every critical .cs file inside the 'Managers', 'Setup', or root directories required to evaluate our StarCraft II worker assignment logic and 'Just In Time Mining' features.

## Output Requirement
Generate a clean markdown table explicitly named 'KimiK3.md'. For every file you determine is necessary, generate its absolute GitHub raw link using the repository structure:
https://githubusercontent.com[Path/To/File.cs]

Do not write code blocks. Only output the KimiK3.md link index table.
